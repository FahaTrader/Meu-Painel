import React from 'react';
import ClientesList from './ClientList';

const ClientesPage = () => {
  return (
    <div style={{ padding: '20px' }}>
      <ClientesList />
    </div>
  );
};

export default ClientesPage;
